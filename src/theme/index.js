import {createTheme} from '@rneui/themed';

const theme = createTheme({
  lightColors: {
    primary: '#305C93',
    secondary: '#121C28',
    background: '#DAD3CD',
  },
  darkColors: {
    primary: '#000',
  },
  mode: 'light',
});

export default theme;
